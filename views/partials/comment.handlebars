<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{User.username}} on {{ comment.date_created }}
    </div>
    <div class="text">
      {{comment}}
    </div>
  </section>
  {{/each}}
</div>
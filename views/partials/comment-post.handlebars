<div class="comments">
  {{#each comment }}
  <section class="comment">
    <div class="meta">
      {{User.username}} on {{ this.date_created }}
    </div>
    <div class="text">
      {{!-- {{comment}} --}}
      {{this.content}}
    </div>
  </section>
  {{/each}}
</div>
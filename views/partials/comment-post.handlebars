<div class="comments">
  {{#each post.comments as }}
  <section class="comment">
    <div class="meta">
      {{User.username}} on {{ comment.date_created }}
    </div>
    <div class="text">
      {{!-- {{comment}} --}}
      {{this.content}}
    </div>
  </section>
  {{/each}}
</div>